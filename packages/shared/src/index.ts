/**
 * @profit-step/shared — barrel export.
 *
 * Canonical home for cross-module utilities, types, and helpers that
 * multiple modules (USER, TIME, CLIENT, MONEY) need. Pilot scope (day 1):
 * phone normalization only. Future additions per MODULE_EXTRACTION_PLAN §2.2.
 */

export * from './utils/phone';
